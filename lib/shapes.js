// Create class constructor for shapes containing similar properties, 
// then extend the class with other constructors.
class Shapes {
  constructor(color) {
    
    this.color = color;
    this.text = text;
  }
    <svg width="500" height="500">
<rect width="175" height="200" fill="" />
  </svg>

}